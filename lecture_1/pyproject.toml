[project]
name = "my_project"
version = "0.1.0"
description = "A Python project with colorama for colored terminal output"
requires-python = ">=3.8"
dependencies = [
    "colorama>=0.4.6",
]

[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

